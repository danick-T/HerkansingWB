import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.projectapplicatieherkansing',
  appName: 'projectapplicatieherkansing',
  webDir: 'dist'
};

export default config;
